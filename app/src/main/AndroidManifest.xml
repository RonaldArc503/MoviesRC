<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:banner="@mipmap/ic_launcher"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.REX">
        <activity
            android:name=".ui.home.HomeActivity"
            android:banner="@mipmap/ic_launcher"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.TvSearchActivity" />
        <activity android:name=".ui.home.TvChannelsActivity" />
        <activity android:name=".ui.detalle.DetalleContenidoActivity" />
        <activity android:name=".ui.detalle.DetalleActivity" />
        <activity
            android:name=".ui.player.PlayerContenidoActivity"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:name=".ui.player.PlayerExoActivity"
            android:screenOrientation="sensorLandscape" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>
